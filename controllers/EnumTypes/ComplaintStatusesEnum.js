const ComplaintStatusesEnum = Object.freeze({
    PENDING: "pending",
    FOLLOWUP: "follow up",
    ONGOING: "ongoing",
    RESOLVED: "resolved",
    REJECTED: "rejected",
})

module.exports = ComplaintStatusesEnum