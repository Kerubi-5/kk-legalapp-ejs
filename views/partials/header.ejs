    <!-- NAVBAR -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">3JBG</a>
          <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <% if (isAuthenticated) { %>
                <li class="nav-item">
                  <a class="nav-link" aria-current="page" href="/dashboard"
                    >Dashboard</a
                  >
                </li>
              <% } %>
              <li class="nav-item">
                <a class="nav-link" href="/about">About</a>
              </li>
            </ul>
            <% if (isAuthenticated) { %>
                <ul class="navbar-nav">
                  <li class="navbar-item"><a href="/users/<%- user_id %> " class="nav-link">Profile</a></li>

                    <li class="navbar-item"><a href="/users/logout" class="nav-link">Logout</a></li>
                </ul>
            <% } else { %>
                <ul class="navbar-nav">
                    <li class="nav-item">
                      <a class="nav-link" href="/users/register">Signup</a>
                    </li>
                    <li class="nav-item"><a class="nav-link" href="/users/login">Login</a></li>
                  </ul>
            <% } %>
          </div>
        </div>
      </nav>